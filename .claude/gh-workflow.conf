# Repo config for the git-pr-workflow skill's gh-workflow.sh.
# Keep VALIDATE_CMDS in sync with the checks expected before pushing a PR.
VALIDATE_CMDS=("cargo fmt --all -- --check" "cargo test --workspace")
ET_PROJECT_NUMBER="1"
ET_PROJECT_OWNER="cmilatinov"
ET_DEFAULT_STATUS="Backlog"
DEFAULT_BASE="main"
